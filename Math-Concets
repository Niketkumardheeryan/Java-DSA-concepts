  1. Input Untill next line or input receiver from system
    - Strings
    Scanner sc = new Scanner(System.in);
        int i=1;
        while (sc.hasNext()) {
        	String s=sc.nextLine();
        	System.out.println(s);
        	
        }
        
     - Arrays 
          Scanner sc=new Scanner();
          while(sc.hasNext()){
           int x= sc.nextINT();
          }
2. String - INt 
    - Interger.toString(x)
    - String.ValueOf
